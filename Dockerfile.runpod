FROM pytorch/pytorch:2.3.0-cuda12.1-cudnn8-runtime

WORKDIR /app

RUN pip install --no-cache-dir \
    runpod \
    numpy \
    pandas \
    scikit-learn \
    structlog

COPY gpu/runpod_worker.py /app/handler.py

CMD ["python", "-u", "handler.py"]
